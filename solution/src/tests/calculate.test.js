import calculate from '../logic/calculate'

describe('calculate', () => {
  it('foo')

  // I am getting tired of writing tests. :)
  // For calculate, you should test each path in the code. That means you
  // will have at least as many tests as `return`s.
})